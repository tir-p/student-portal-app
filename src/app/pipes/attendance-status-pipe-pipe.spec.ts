import { AttendanceStatusPipePipe } from './attendance-status-pipe-pipe';

describe('AttendanceStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AttendanceStatusPipePipe();
    expect(pipe).toBeTruthy();
  });
});
