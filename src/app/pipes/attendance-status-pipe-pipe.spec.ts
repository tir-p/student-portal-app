import { AttendanceStatusPipe } from './attendance-status-pipe-pipe';

describe('AttendanceStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AttendanceStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
